RZPM_BEGIN

RZPM_GIT "https://github.com/radareorg/radare2-extras"
RZPM_DESC "[util] crashlog parser"

RZPM_INSTALL() {
	F=${RZPM_BINDIR}/r2crash
	ln -fs ${PWD}/crashlog/r2crash.js $F
	chmod +x $F
}

RZPM_UNINSTALL() {
	F=${RZPM_BINDIR}/r2crash
	rm -f "$F"
}

RZPM_END
