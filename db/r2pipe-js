RZPM_BEGIN

RZPM_DESC "[r2pipe] r2pipe API for NodeJS"

RZPM_INSTALL() {
	if [ "${GLOBAL}" = 1 ]; then
		${SUDO} npm install -g r2pipe
	else
		npm install r2pipe
	fi
}

RZPM_UNINSTALL() {
	if [ "${GLOBAL}" = 1 ]; then
		${SUDO} npm uninstall -g r2pipe
	else
		npm uninstall r2pipe
	fi
}

RZPM_END
